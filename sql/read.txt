huan:
    1、大家写的所有的控制器都必须加控制器后缀 Controller
    2、所有的控制器必须继承BaseController
    3、我已经建立了所有的模型类，位于common\model
    4、写代码之前必须先获取更新，以免覆盖别人的代码
    5、在提交之前必须先在项目名（SpeedyShopping）右键，选择Git,选择+add,然后在点击右上角的提交按钮
    6、获取最新项目之后，必须先导入数据库